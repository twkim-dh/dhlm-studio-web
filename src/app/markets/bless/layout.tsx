import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Bless My Stock — Fortune Buddha | DHLM Studio",
  description: "Light incense before the Fortune Buddha. 100% donated to St. Jude Children's Research Hospital.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
