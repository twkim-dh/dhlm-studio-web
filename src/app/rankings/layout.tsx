import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "World Rankings — Billionaires, Companies, GDP, Population | DHLM Studio",
  description: "Global rankings: billionaire net worth, largest companies by market cap, GDP by country, population, and sports salaries.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
